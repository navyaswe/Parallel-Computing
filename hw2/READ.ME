erial prefix sum is provided for you. Implement the two parallel versions discussed in class, one using a O(nlogn) algorithm, and the other using a O(n) algorithm.

2) Experiment with your code to see how well it performs with different number of threads.

3) Write up a small (one page) report outlining your findings (use Latex). Use Figures when possible.

4) This is due Wednesday, November 1st, at 11:59 PM.

